#ifndef __quarto_handle_rounds_h__
#define __quarto_handle_rounds_h__

#include "spieldata.h"

void handle_rounds(int sock_stream, spielfeld *shm_fd, int pipe_fd);

#endif //__quarto_handle_rounds_h__

